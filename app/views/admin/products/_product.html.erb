<li>
  <div class="album-infor">
    <div class="ab">
      <%= link_to image_tag(product.img, size: "150x190", class: "album-image" ), admin_product_path(product.id) %>
    </div>
    <div class="text-center album-name"><%= product.name %></div>
    <div class="text-center"><%= product.price %></div>
    <div class="text-center"><%= product.discount %></div>
  </div>
</li>
